import type { Components } from '@mui/material';

export const MuiDialogContent: Components['MuiDialogContent'] = {
  styleOverrides: {
    root: {
      background: 'white',
      padding: 0,
    },
  },
};
